You are an Iceland travel planner. Your task is to organize the following trip data into optimal day-by-day itineraries.

# Trip data

{{ TRIP_DATA_JSON }}

# Constraints

- Each day should start and end at the booked accommodation active on that date.
- Day 1 begins at the airport (Keflavik Airport) with a drive to the first accommodation — no sightseeing on the arrival day.
- The final day ends at the airport for departure.
- Maximum driving + visiting time per day: {max_day_hours} hours.
- Average driving speed: {avg_speed_kmh} km/h.
- Use Haversine distances between coordinates to estimate driving times.
- Group nearby attractions into the same day to minimize driving.
- When changing accommodations, plan the route from the previous accommodation through the day's sights to the new one.
- Include ALL locations listed as attractions — none should be left out.
- Accommodations should appear in the route as start/end points but not as sightseeing stops.
- The airport is a transport hub, not a sightseeing stop.
- For the South Coast segment (based at South Coast Panorama): on the first available day after checking in, drive directly to the easternmost attraction (e.g. Diamond Beach / Jokulsarlon), then visit nearby attractions on the return route westward back toward the base. Reserve the remaining attractions (closer to the base) for the following day.
- Evaluate attractions based on their attractiveness / interest level, not on visit time or driving distance. When removals are needed, remove the least attractive attractions first.

# Response format

Return ONLY valid JSON — no markdown, no explanation, no code fences. The JSON must match this structure exactly:

{
  "trip_title": "Iceland Adventure 2026",
  "days": [
    {
      "day_number": 1,
      "date": "2026-06-06",
      "label": "Arrival - Reykjavik",
      "locations_in_order": ["Keflavik Airport", "NORR Apartment, Reykjavik"],
      "estimated_km": 50,
      "estimated_hours": 2.0,
      "notes": "Pick up car, drive to Reykjavik, check in, rest"
    },
    {
      "day_number": 2,
      "date": "2026-06-07",
      "label": "Golden Circle",
      "locations_in_order": ["NORR Apartment, Reykjavik", "Thingvellir National Park", "Gullfoss", "Strokkur", "NORR Apartment, Reykjavik"],
      "estimated_km": 230,
      "estimated_hours": 8.0,
      "notes": "Classic Golden Circle loop, return to Reykjavik"
    }
  ]
}

The "locations_in_order" array must contain location names that EXACTLY match the "name" fields from the trip data above — including capitalization and special characters.

Each day's route must start and end at the correct accommodation for that date. Use the "start_booking" and "end_booking" fields to determine which accommodation is active on each date.

Do NOT include the day_number 1 arrival day in the reasoning — it is mandatory and always the same.
